services:
  app:
    ports:
      - "127.0.0.1:5001:5000"  # Only listen on localhost, Apache will proxy

  db:
    ports: []  # Don't expose MySQL externally

  redis:
    ports: []  # Don't expose Redis externally
